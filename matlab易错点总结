易错点一：
matlab是按列读取，三通道矩阵的读取和存储都是一列一列读取或者存储的（:,:,1） (:,:,2)  (:,:,3)
                                      其中第一个（:,:,1）的读取顺序是 （:,1:,1）...
matab主要是用作矩阵的运算，按列操作更为方便

易错点二：
matlab中使用imread(路径)读取出来的图像矩阵的数据类型是uint 
自己创造一个矩阵元素类型是double
如果需要用im2bw函数处理自己的矩阵，注意要使用uint将自己的矩阵元素的类型转换成uint类型，才能操作成功
                                     
