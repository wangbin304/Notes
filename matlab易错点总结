易错点一：
matlab是按列读取，三通道矩阵的读取和存储都是一列一列读取或者存储的（:,:,1） (:,:,2)  (:,:,3)
                                      其中第一个（:,:,1）的读取顺序是 （:,1:,1）...
matab主要是用作矩阵的运算，按列操作更为方便

易错点二：
                                      
                                     
